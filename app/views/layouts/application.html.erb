<!DOCTYPE html>
<html>
<head>
  <title>AlcoB2c</title>
  <%= csrf_meta_tags %>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500&display=swap" rel="stylesheet">

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>

<% if params[:action] != "caller_login" %>

  <header>
    <div class="head">
        <span class="logo"
        ><%= image_tag("alco_eats.png",:class => "img_preview") %>
        </span>
      <div class="account-user">
        <span><%= session[:name] %></span><br>
        <span><%= session[:email] %></span>
      </div>
    </div>
  </header>
  <main>
    <div class="container-fluid section">
      <div class="row">

        <% if (params[:action] != "caller_login") && (params[:action] != "customer_payment_page") && (params[:action] != "payment_acknowledgement")  %>
          <div class="col-md-2 sideBar">
            <div class="row sidebar-menu">
        <span><h4>Call Center</h4><span>
            </div>
            <hr>
            <div class="row mt-5 cust-info">
              <a href="/search_customer">Home</a>
            </div>
            <div class="mt-3 order-info">
              <ul>
                <li><a>Purchase Order</a>
                  <ul class="po-list">
                    <li><a href="/product_selling">- Create Purchase Order</a></li>
                    <li><a href="/order">- View Purchase Order</a></li>
                  </ul>
                </li>
              </ul>
            </div>
            <div class="order-info">
              <a href="#">Create New Customer</a>
            </div>
            <div class="logout-btn">
              <button class="btn"><a href="/logout">Logout</a></button>
            </div>
          </div>
        <%end %>
        <div class="col-md-10" >
          <div class="content">
<% end %>
<%= yield %>
<% if params[:action] != "caller_login" %>
  </div>
  </div>
  </div>
  </div>
  </main>
  <footer><div class="copyright">Copyright@Alco Foods PVT LTD.</div></footer>
  </body>
  </html>

  </body>
  </html>
<% end %>
